# workbar 用户文档规划

本文档定义 workbar 用户文档的整体范围、文章结构和编写顺序。全套正文初稿与第一轮事实、文案精修已于 2026-08-09 完成；首批配图、本地单页预览，以及面向 `docs.workbar.ai` 的多页面生产构建和部署资料也已完成。后续继续处理剩余脱敏截图、服务器发布与线上验收。

## 1. 文档目标

workbar 文档主要服务三类读者：

1. 第一次使用 AI 工具，希望按步骤完成连接的新用户；
2. 已经在使用 workbar Code、Claude Code 或其他兼容工具，需要解决配置问题的用户；
3. 需要自行接入 OpenAI、Claude 或 Gemini 兼容接口的开发者。

所有文章遵循以下原则：

- 先告诉读者能完成什么，再解释必要概念；
- 优先使用界面中的中文名称，必要时再补充英文技术字段；
- 一个步骤只完成一个明确动作，并给出可检查的完成标志；
- 地址、密钥和模型名称分别说明，避免把网站地址、Base URL 和接口路径混为一谈；
- 示例只使用占位密钥，不展示真实账号、邮箱、余额、项目路径或 API 密钥；
- 会随系统配置变化的金额、模型和分组，以用户当前页面显示为准。

## 2. 整体信息架构

### 开始使用

- [快速开始](content/getting-started/quick-start.md)：从注册到在 workbar Code 中完成第一次任务。

### 连接 AI 工具

- [使用 workbar Code](content/tools/workbar-code.md)：下载安装、连接账号、选择项目和权限模式、日常使用。
- [在 Claude Code 中使用 workbar](content/tools/claude-code.md)：配置服务地址和密钥，并完成验证。
- [连接其他兼容工具](content/tools/compatible-tools.md)：判断工具使用的接口格式，并填写正确地址。

### 管理访问与模型

- [管理 API 密钥](content/keys/manage-api-keys.md)：创建、限制、停用、更换和安全保存密钥。
- [模型、分组与自动选择](content/models/models-and-auto-routing.md)：理解模型可用范围、指定分组和自动分组。

### 账号与费用

- [余额、价格与使用记录](content/billing/balance-pricing-and-usage.md)：查看价格、充值、核对用量和推荐奖励。
- [账号与安全](content/account/account-and-security.md)：登录、Passkey、双重身份验证和账号安全。

### 兼容接口

- [API 接入概览](content/api/index.md)：选择合适的兼容格式并准备接入信息。
- [OpenAI 兼容接口](content/api/openai-compatible.md)
- [Claude 兼容接口](content/api/claude-compatible.md)
- [Gemini 兼容接口](content/api/gemini-compatible.md)

### 解决问题

- [常见问题排查](content/troubleshooting/common-problems.md)：按现象定位密钥、模型、余额、限流和服务错误。

## 3. 每篇结构梳理

### 《快速开始》

`适合谁 → 开始前准备 → 两个必要概念 → 注册登录 → 创建 API 密钥 → 下载 Code → 连接账号 → 选择项目与模型 → 第一次任务 → 未成功时怎么办 → 下一步`

保留现有正文和已经验收的 API 密钥截图。它只负责让新用户完成一次完整闭环，不在这里展开所有高级设置。

### 《使用 workbar Code》

`能完成什么 → 安装与启动 → 首次连接授权 → 选择项目 → 选择模型 → 四种权限模式 → 完成日常任务 → 同步密钥和模型 → 本地数据与隐私 → 常见问题 → 相关文档`

重点解释 Code 本地页面与 workbar 模型服务的区别，以及不同权限模式会允许哪些操作。

### 《在 Claude Code 中使用 workbar》

`适用场景 → 准备账号和密钥 → 安装 Claude Code → 配置环境变量 → 启动并验证 → 更换密钥或模型 → 安全注意事项 → 常见错误 → 相关文档`

只介绍 Claude Code 当前实际需要的字段，不将其他 Anthropic 客户端的配置方式混在同一套步骤中。

### 《连接其他兼容工具》

`先判断工具支持的格式 → 准备共同信息 → OpenAI 格式怎么填 → Claude 格式怎么填 → Gemini 格式怎么填 → 如何验证 → 常见填错方式 → 进入对应接口参考`

这篇负责选择路径，不为每一个第三方工具维护易过期的逐按钮教程。

### 《管理 API 密钥》

`API 密钥是什么 → 什么时候应创建多把密钥 → 创建步骤 → 名称/分组/到期/配额/模型限制 → 查看与复制 → 编辑、停用和删除 → 更换泄露密钥 → 安全清单 → 相关文档`

需要明确“无限配额”不是无限余额，“模型限制留空”不是没有模型。

### 《模型、分组与自动选择》

`模型和分组分别是什么 → 自动分组如何工作 → 指定分组有什么变化 → 模型限制如何叠加 → 模型为什么暂时不可用 → 常见选择场景 → 如何检查 → 相关文档`

这篇只解释用户能看到和控制的行为，不公开后台渠道、采购或调度细节。

### 《余额、价格与使用记录》

`先理解余额与模型价格 → 在模型广场查看价格 → 在钱包充值 → 查看订单 → 在使用日志核对消耗 → 余额不足怎么办 → 推荐奖励如何进入余额 → 费用安全建议 → 相关文档`

金额、支付方式和奖励数值都以用户当前页面显示为准；文档只固定流程和字段含义。

### 《账号与安全》

`注册与登录 → 找回密码 → Passkey → 双重身份验证 → 密码与密钥的区别 → 保护账号 → 登录异常处理 → 账号操作边界 → 相关文档`

账号删除、邮箱释放等不可逆行为只有在真实规则核对后才写入，不根据一般网站经验推断。

### 《API 接入概览》

`什么时候需要直接接入 → 准备 Base URL/密钥/模型名 → 选择兼容格式 → 三种格式的差异 → 最小验证顺序 → 通用错误与安全原则 → 进入协议页`

它是三个协议页的入口，不重复列出完整请求体。

### 《OpenAI 兼容接口》

`适用 SDK 和工具 → Base URL → Bearer 身份验证 → Chat Completions → Responses → 流式输出 → 模型名 → 错误处理 → 最小示例 → 相关文档`

### 《Claude 兼容接口》

`适用 SDK 和工具 → Base URL → x-api-key 身份验证 → Messages 请求 → 版本头 → 流式输出 → 模型名 → 错误处理 → 最小示例 → 相关文档`

### 《Gemini 兼容接口》

`适用 SDK 和工具 → Base URL/版本路径 → x-goog-api-key 身份验证 → generateContent → 流式输出 → 模型名 → 错误处理 → 最小示例 → 相关文档`

### 《常见问题排查》

`先收集哪些信息 → Code 页面打不开 → 无法连接账号 → 密钥无效 → 看不到模型 → 余额不足 → 429 → 5xx → 结果异常或中断 → 如何安全反馈 → 相关文档`

按用户看到的现象组织，不要求用户先知道 HTTP 状态码属于哪一类问题。

## 4. 推荐阅读路径

### 第一次使用 workbar

`快速开始 → 使用 workbar Code → 管理 API 密钥 → 模型、分组与自动选择`

### 已经有自己的 AI 工具

`快速开始中的账号与密钥步骤 → 连接其他兼容工具 → 对应的兼容接口页`

### 使用 Claude Code

`管理 API 密钥 → 在 Claude Code 中使用 workbar → 常见问题排查`

### 自己编写程序

`API 接入概览 → 对应的兼容接口页 → 余额、价格与使用记录`

## 5. 每篇文章的通用模板

### 操作指南

适用于快速开始、工具连接、密钥管理和账号设置：

1. 这篇文章能帮你完成什么；
2. 开始前准备；
3. 按界面顺序完成操作；
4. 每个关键阶段的完成标志；
5. 常见问题与恢复方法；
6. 下一步阅读。

### 概念说明

适用于模型分组、计费和安全说明：

1. 先用一句话说明概念；
2. 什么时候需要关注；
3. 关键规则；
4. 常见场景；
5. 容易误解的地方；
6. 相关操作入口。

### 接口参考

适用于 OpenAI、Claude 和 Gemini 兼容接口：

1. 适用工具或 SDK；
2. Base URL 与身份验证方式；
3. 最小请求示例；
4. 流式输出或常用参数；
5. 常见错误；
6. 安全注意事项与相关指南。

## 6. 统一术语

| 统一写法 | 说明 |
| --- | --- |
| workbar | 品牌名始终使用小写。 |
| API 密钥 | 用户界面中的访问凭据；首次出现时可说明通常以 `sk-` 开头。 |
| Base URL | 工具或 SDK 要求填写的接口根地址，不等同于浏览器中打开的网站页面。 |
| 模型 | 实际完成对话、写作、编程等任务的 AI 服务。 |
| 分组 | 一组可用模型与计费规则的组合。 |
| 自动分组 | 用户不指定具体分组时，由系统按后台设置的顺序选择可用分组。 |
| 模型限制 | 为某一把 API 密钥进一步限定可用模型；留空表示不额外限制。 |
| 余额 | 用户可用于模型调用的账户额度，金额显示以当前系统结算货币为准。 |

## 7. 配图规划

配图遵循“只在能减少操作歧义时使用”的原则。首批线上界面截图已经完成脱敏并接入正文；仍需逐张确认剩余图片是否确有必要，避免为每篇文章强行配图。

| 优先级 | 文章 | 建议配图 | 当前状态 |
| --- | --- | --- | --- |
| P0 | 快速开始 | API 密钥入口与创建表单、Code 下载入口、首次连接、主界面三处关键控件、第一次任务完成状态。 | API 密钥和 Code 下载入口已接入；本地 Code 状态待制作。 |
| P0 | 管理 API 密钥 | API 密钥列表、创建表单、停用与编辑入口。 | 现有 API 密钥截图可复用；发布前继续检查脱敏。 |
| P0 | 使用 workbar Code | Code 下载入口、首次授权、模型同步、项目与权限模式选择。 | Code 下载入口已接入；本地 Code 状态待制作。 |
| P1 | Claude Code | 配置位置和验证成功状态；不展示完整密钥。 | 待评估是否仍需截图，优先保留可复制命令。 |
| P1 | 余额、价格与使用记录 | 模型广场、钱包充值区、订单历史、使用日志、推荐奖励转入入口。 | 模型广场和钱包充值区已接入；其余按实际必要性补充。 |
| P1 | 模型、分组与自动选择 | 模型广场、密钥分组与模型限制的关系。 | 模型广场已接入；密钥创建表单可复用。 |
| P2 | 账号与安全 | Passkey 与双重身份验证入口。 | 待制作。 |
| P2 | 常见问题排查 | 仅为难以通过文字定位的界面状态配图。 | 按需制作，不主动制造错误状态。 |

截图发布前必须检查：用户名、头像、邮箱、邀请链接、完整密钥、余额、订单号、本地路径和私人项目内容是否已经隐藏。

## 8. 编写与验收阶段

### 阶段一：全套初稿

**状态：已于 2026-08-09 完成。**

- 完成所有文章的主要步骤、解释和交叉链接；
- 复用已经完成的《快速开始》及其现有图片；
- 不以配图缺失阻塞正文；
- 不写入尚未核实的固定模型、价格或奖励数值。

### 阶段二：逐篇事实与文案优化

**状态：已于 2026-08-09 完成第一轮全量精修。**

- 从《快速开始》开始逐篇核对线上界面；
- 将长句改为新手容易执行的动作；
- 核对按钮名称、页面路径、协议地址和错误说明；
- 删除重复内容，让每篇文章只承担一个主要目的。

建议优化顺序为：

1. 《快速开始》；
2. 《管理 API 密钥》与《使用 workbar Code》；
3. 《模型、分组与自动选择》与《余额、价格与使用记录》；
4. Claude Code、其他兼容工具和三种 API 接口；
5. 《账号与安全》与《常见问题排查》。

本轮已覆盖文档首页、《快速开始》、三篇工具指南、API 密钥与模型分组、余额计费、账号安全、常见问题，以及 API 概览和 OpenAI、Claude、Gemini 三篇兼容接口。正文已统一新手表达、地址层级、安全边界和交叉链接；没有把配图缺失留成正文占位。

发布前需要重点人工复核：

- 用户自行注销后的账号、用户名和邮箱处理方式，必须与隐私政策和页面提示一致；
- “访问令牌”是系统管理凭据，不得与模型调用的 API 密钥混淆；
- 推荐奖励的触发条件、到账方式、最低转移金额和当前数值；
- 第三方工具是否会自动追加 `/v1`、`/v1/messages` 或 `/v1beta`；
- 模型、分组、价格、支付方式和安全选项是否仍与线上配置一致。

### 阶段三：配图与发布检查

**状态：进行中；首批配图、本地预览、正式多页面构建和服务器部署资料已于 2026-08-09 完成，尚待线上发布。**

- 已接入 API 密钥入口与创建表单、Code 下载入口、模型广场和钱包充值区；
- 已在 `preview/` 生成单文件本地文档站：默认首页按目标分流，支持 5 组折叠导航、14 篇内容切换、站内搜索、文章目录、代码复制、配图放大、深浅色和移动端布局；
- 本地预览已完成桌面端与移动端、搜索跳转、配图加载和放大检查；构建过程会校验站内 Markdown 链接和图片路径；
- 已确定 `docs.workbar.ai` 独立静态站方案，并在 `dist/` 生成 14 篇真实目录路由页面、自包含且带内容哈希的 CSS/JS/图片、搜索索引、404、robots、sitemap、canonical、Open Graph、favicon 和 manifest；
- 已在 `deploy/` 准备同一 VPS 上的 Nginx 虚拟主机、安全头、缓存策略、版本目录原子发布、回滚和验收手册；文档发布不进入 New API 镜像，也不使用 `FRONTEND_BASE_URL`；
- 继续按配图规划制作必要的本地 Code 状态，并逐张完成脱敏和裁切；
- 检查浅色与深色界面下是否仍然清晰；
- 验证全部站内链接、外部链接和示例；
- 将 `dist/` 发布到 `docs.workbar.ai`，完成 DNS、TLS、服务器 `nginx -t`、后台文档链接、真实缓存与安全响应头检查，以及线上回滚演练。

## 9. 当前完成边界

当前已完成 Markdown 正文、导航与首页、第一轮全量精修、首批线上界面配图、单页本地预览、正式多页面静态构建，以及 `docs.workbar.ai` 独立部署和回滚资料。本地产物和交互回归已经通过，但没有操作线上 DNS、证书、Nginx 或 workbar 后台配置，因此正式发布仍未完成。尚未完成的范围包括：按第 7 节补充确有必要的本地 Code 与其他界面状态、部署并验收真实文档域名，以及后续版本切换或多语言能力。本轮入库截图已经完成脱敏复核；正式发布前仍需基于当时的线上界面和系统配置，再次检查截图像素，以及会变化的模型、价格、分组、支付方式、奖励和安全选项。
